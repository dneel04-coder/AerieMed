org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableResourceOptimizations=true
# Enables R8's optimized resource shrinking (treats resources as part of the
# code reference graph so more unused resources are removed). Automatic in
# AGP 9.0+; this flag backports it to AGP 8.6-9.0. Requires isShrinkResources
# = true in buildTypes.release (already set in app/build.gradle.kts).
android.r8.optimizedResourceShrinking=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
